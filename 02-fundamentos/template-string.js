const nombre = 'DeadPool';

const real = 'Wade Winston';

const normal = nombre + ' ' + real;
const template = `${nombre} ${real} ${5*5}`;

console.log(normal);

console.log(template);


const html = `
<h1>Hola</h1>
<p>Mundo</p>
`;

console.log(html);